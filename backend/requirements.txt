fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
dependency-injector>=4.42

# Vector store
qdrant-client>=1.9
openai>=1.30

# LLM / gateway
litellm>=1.40

# MCP — pinned to 2.0+ since our code targets streamable_http_client's
# 2.0 signature (renamed from streamablehttp_client in 1.x, and headers
# now go through an explicit httpx2.AsyncClient — confirmed via a live
# install that pulled 2.0.0 with requirements.txt's old `mcp>=1.0`).
mcp>=2.0
httpx2>=2.9

# Tracing
opentelemetry-sdk>=1.25
opentelemetry-exporter-otlp>=1.25
opentelemetry-instrumentation-fastapi>=0.46b0
# openllmetry / openinference — package name to be confirmed against latest docs
traceloop-sdk>=0.28

# Evals — LLM-as-judge implemented directly against provider SDKs (see
# infrastructure/evals/ragas_evaluator.py for why: the `ragas` PyPI
# package currently has a broken import chain incompatible with
# LangChain v1.0, verified during implementation).
deepeval>=1.0
anthropic>=0.34

# Testing
pytest>=8.2
pytest-asyncio>=0.23
pytest-mock>=3.14
httpx>=0.27

# Dev tooling
ruff>=0.5
